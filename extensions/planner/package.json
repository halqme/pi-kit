{
  "name": "@halqme/planner",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "bun run typecheck && bun test --parallel",
    "typecheck": "tsc --noEmit",
    "test": "node --test",
    "dev": "pi -e ./index.ts"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "catalog:",
    "@earendil-works/pi-coding-agent": "catalog:",
    "@earendil-works/pi-tui": "catalog:",
    "@halqme/plan-state": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
