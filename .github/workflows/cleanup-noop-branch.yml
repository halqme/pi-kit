name: Cleanup noop branch

on:
  push:
    branches:
      - vnext-restructure

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: git push origin --delete noop-check
